# Global options
{
    email {$EMAIL}
    # DuckDNS module is built-in via caddy-dns/duckdns if needed, but basic works
}

# Homepage
homepage.{$DOMAIN} {
    reverse_proxy localhost:${PORT_DASH_HTTP}
}

# Jellyfin
jellyfin.{$DOMAIN} {
    reverse_proxy localhost:${PORT_HTPC_HTTP}
}

# Immich
immich.{$DOMAIN} {
    reverse_proxy localhost:${PORT_IMMICH}
}

# PriceBuddy
pricebuddy.{$DOMAIN} {
    reverse_proxy localhost:${PORT_PRICEBUDDY}
}
